import {createSlice,PayloadAction} from '@reduxjs/toolkit';import {load,save} from '../../app/storage';type State={theme:'light'|'dark';language:'en'|'fr'|'ar'};const initial:State=load('bt_settings',{theme:'light',language:'en'});const slice=createSlice({name:'settings',initialState:initial,reducers:{setTheme:(s,a:PayloadAction<State['theme']>)=>{s.theme=a.payload;save('bt_settings',s)},setLanguage:(s,a:PayloadAction<State['language']>)=>{s.language=a.payload;save('bt_settings',s)}}});export const {setTheme,setLanguage}=slice.actions;export default slice.reducer;
