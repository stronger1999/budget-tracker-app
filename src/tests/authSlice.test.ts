import {describe,it,expect} from 'vitest';import reducer,{register,login} from '../features/auth/authSlice';describe('auth slice',()=>{it('registers user',()=>{const s=reducer(undefined,register({name:'A',email:'a@a.com',password:'123456'}));expect(s.currentUser?.email).toBe('a@a.com')});it('logs in demo',()=>{const s=reducer(undefined,login({email:'demo@budget.app',password:'password123'}));expect(s.currentUser?.email).toBe('demo@budget.app')})})